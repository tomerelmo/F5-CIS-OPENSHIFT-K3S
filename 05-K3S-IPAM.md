# Here we are installing the ipam on the openshift cluster

# we will clone the repo of the ipam 
# we will create the persist volume for the ipam
# we will make sure that ipam image exists and ready to use 
# we will configure the ipam values such as the addresses ranges and the label for each range (there are DEV and PROD)
# commands to view and verify that the ipam is wroking 

