# open one of the F5 for each pair
# go into the data center section - under this object we define a gourp of servers which need to be "alive" to mark the site as OK
# create EDNS CRD yaml for making the DNS record 
# make sure the record responding to dns
# explain that on the other cluster we will make similar record for creating GLSB solution
